# Kiro IDE files
.kiro/

# Python caching
__pycache__/
*.pyc
*.pyo
.venv/
env/
.pytest_cache/
.hypothesis/

# Large data files — download separately (see README)
*.csv
glove*.txt

# Checkpoints are committed so recruiters can run the demo without retraining.
# If they grow too large, consider Git LFS: https://git-lfs.github.com
# checkpoints/   ← intentionally NOT ignored
